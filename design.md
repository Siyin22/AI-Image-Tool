# 前端审查与设计说明

## 本次审查结论

原版主要问题及处理：

| 问题 | 本次处理 |
| --- | --- |
| 桌面端参数、任务状态和结果纵向堆叠，图片不在主要视野内 | 加宽的双栏工作台：左侧创作、右侧画布上下对齐；参数取消内部滚动，图库独立为页面视图 |
| 空任务详情占用固定的 248px 高度 | 默认折叠详情，生成开始后自动展开；状态摘要始终可见 |
| 参考图布局同时由 CSS、HTML 内联样式、JS 强制设置 | 移除内联样式和 `enforceReferenceActionLayout`，由 CSS 统一控制 |
| 保存提示词预设后没有使用入口 | 添加预设选择器，支持恢复提示词、负面提示词与风格 |
| 图库搜索无结果和图库为空显示相同文案 | 区分两类空状态，显示当前筛选数量与总量 |
| 图片无法放大查看 | 原生 dialog 预览，支持 Esc 关闭、下载与参数复用 |
| 搜索、筛选、弹窗关闭等缺少明确的辅助标签 | 添加可访问名称、焦点样式、状态播报与减弱动画适配 |
| toast 叠在同一位置且会被原生弹窗遮住 | 通知区域按条目堆叠，弹窗打开时放入顶层弹窗内 |
| 设置表单中 URL 无效会阻止关闭按钮提交 | 关闭按钮使用 `formnovalidate` |

## 视觉与布局

- 雾蓝背景 `#f3f6f9`、白色面板、雾蓝主色 `#6485a6`，边框、插画和辅助色统一为蓝灰色系。
- 字体优先使用本机霞鹜文楷（`LXGW WenKai` / `霞鹜文楷`），不可用时回退到微软雅黑（`Microsoft YaHei` / `微软雅黑`）。无外部字体、图片或 CDN 运行依赖。
- 空画布采用内联 SVG 插画，避免把示例图片误认为真实生成结果。
- 常用流程为描述画面、调整画面设置。风格、比例、尺寸、数量、质量和预设按三列排列；参考图单独折叠，负面提示词、尺寸说明与高级参数放入“更多设置”。
- 超过 1000px 使用接近等宽的双栏，高度随表单内容自然展开；取消固定视口高度和左栏内部滚动。画布与表单保持上下对齐，较窄屏幕改为单栏。
- 创作与图库使用 `#/create`、`#/gallery` 路由切换，同一时间只显示一个页面视图。支持直达、刷新和浏览器历史；切换不会销毁草稿或中断任务，图库复用参数自动返回创作页。
- 创作页移除介绍横幅，导航与工作台仅保留 12–16px 间距；较矮桌面视口适度收紧分区间距，默认表单在 1920×1080 和 1536×780 视口内完整显示。
- 保留现有 DOM ID 与平台请求协议，不引入前端框架或构建步骤。

## 新增交互

- 灵感示例填入提示词与风格；覆盖已有内容前确认。
- 上传区支持拖放，复用原有服务商数量与大小限制；先检查图片 MIME 类型。
- Ctrl / Cmd + Enter 开始生成，弹窗打开、正在生成或位于图库页时不触发。
- 图片卡片使用原生按钮，键盘可打开预览；预览内可下载、复用。
- 预设选择器展示本地保存的提示词预设。

## 验证

1. `node --check app.js`
2. `node --test test/*.test.js`：10 项通过，覆盖模型能力、百炼路由、凭据存储和参考图限制。
3. 浏览器冒烟测试：

   ```powershell
   npm install --prefix .cache/ui-check --no-package-lock --no-audit --no-fund playwright
   node .cache/ui-check/node_modules/playwright/cli.js install chromium
   node test/ui-smoke.cjs
   ```

浏览器检查覆盖 1440、1024、800、768、390、320px 宽度，无横向溢出；另验证 1920×1080、1536×703、1024×768、1440×900 下两栏上下对齐、创作区无内部滚动，以及任务详情展开和生成结果返回后的对齐；验证 1440×900 默认常用表单无需滚动即可触及生成按钮、图库独立导航、刷新、前进后退、草稿保留、图库复用回到创作页、生成过程中切换页面、预设保存和恢复、无效 URL 下关闭设置、生成成功和失败、图片预览、图库搜索与批量删除、快捷键、参考图上传和移除、非法文件拖放。未发现页面 JavaScript 异常。截图输出到忽略目录 `.cache/ui-desktop.png` 、`.cache/ui-mobile.png` 和 `.cache/ui-gallery.png`。

浏览器测试使用临时静态服务与模拟 API，不读取用户配置，也不调用付费生成服务。真实平台返回、长时间任务和不同模型的实际图像效果仍需使用有效配置验收。

## 后续维护

`app.js` 仍包含原有的配置迁移、平台适配、请求与渲染逻辑。本次聚焦前端界面和相关交互，没有迁移存储格式或拆分后端协议；后续可单独拆分配置、任务与图库模块。
